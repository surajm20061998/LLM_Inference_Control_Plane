---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: modeldeployments.inference.llmcp.io
spec:
  group: inference.llmcp.io
  names:
    categories:
    - inference
    kind: ModelDeployment
    listKind: ModelDeploymentList
    plural: modeldeployments
    shortNames:
    - md
    - mdep
    singular: modeldeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.engine.type
      name: Engine
      type: string
    - jsonPath: .spec.model.name
      name: Model
      type: string
    - jsonPath: .spec.replicas
      name: Desired
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.stableRevision
      name: Revision
      priority: 1
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ModelDeployment is a declarative deployment of an LLM inference server:
          it owns the serving workload, health-checks it, and reports a status other
          controllers (and an HPA, via the /scale subresource) can act on.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec is the desired state.
            properties:
              engine:
                description: Engine selects and configures the inference server.
                properties:
                  apiKeySecretRef:
                    description: APIKeySecretRef supplies an API key the engine will
                      require on requests.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  contextSize:
                    default: 4096
                    description: ContextSize is the maximum context window, in tokens.
                    format: int32
                    minimum: 256
                    type: integer
                  env:
                    description: Env are additional environment variables for the
                      engine container.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  extraArgs:
                    description: |-
                      ExtraArgs are appended verbatim to the engine's command line, after every
                      argument the profile generates.
                    items:
                      type: string
                    type: array
                  image:
                    description: |-
                      Image overrides the engine profile's default container image.

                      This is also the supported way to substitute a deterministic stub server
                      in tests and CI: the stub is selected here rather than through a
                      dedicated enum value, so the real profile's argument-building code path
                      is exercised rather than bypassed.
                    type: string
                  imagePullPolicy:
                    default: IfNotPresent
                    description: ImagePullPolicy for the engine image.
                    enum:
                    - Always
                    - IfNotPresent
                    - Never
                    type: string
                  maxConcurrency:
                    default: 4
                    description: |-
                      MaxConcurrency is how many requests the engine may process concurrently
                      (llama.cpp's --parallel).

                      It also determines when the engine starts queueing, which is the signal
                      autoscaling is driven from — so it is a capacity knob, not just a
                      performance one.
                    format: int32
                    minimum: 1
                    type: integer
                  resources:
                    description: |-
                      Resources are the engine container's compute resources. Setting a CPU
                      limit is strongly recommended: it is what Threads is derived from.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  threads:
                    description: |-
                      Threads is the engine's worker thread count (llama.cpp's -t).

                      When unset it is DERIVED from the container's CPU limit. This matters
                      more than it looks: llama.cpp reads the host's /proc and is unaware of
                      cgroup limits, so left alone every replica spawns one thread per host
                      core. Several replicas on one node then oversubscribe the CPU badly
                      enough that latency percentiles become noise — which in turn makes
                      metric-driven canary analysis produce false rollbacks.
                    format: int32
                    minimum: 1
                    type: integer
                  type:
                    default: llamacpp
                    description: Type selects the engine implementation.
                    enum:
                    - llamacpp
                    type: string
                type: object
              model:
                description: Model identifies what to serve and where its weights
                  come from.
                properties:
                  name:
                    description: |-
                      Name is the model identifier clients pass as "model" in OpenAI-compatible
                      requests, echoed at GET /v1/models. It is also the `model` label on every
                      metric this project emits, so keep it stable and low-cardinality.
                    maxLength: 253
                    minLength: 1
                    type: string
                  source:
                    description: Source is where the model weights come from.
                    properties:
                      huggingFace:
                        description: |-
                          HuggingFace downloads weights from the Hugging Face Hub at pod start.

                          Convenient, but startup is non-deterministic, every replica re-downloads
                          on every restart, and it is subject to upstream availability and rate
                          limits. Not implemented yet.
                        properties:
                          file:
                            description: |-
                              File is the specific file to fetch, e.g. "Qwen3-0.6B-Q4_K_M.gguf".
                              Required for GGUF engines; omit for engines that consume a whole repo.
                            type: string
                          repo:
                            description: Repo is the Hub repository, e.g. "unsloth/Qwen3-0.6B-GGUF".
                            pattern: ^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$
                            type: string
                          tokenSecretRef:
                            description: TokenSecretRef references a Hugging Face
                              token for gated repositories.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - repo
                        type: object
                      image:
                        description: |-
                          Image mounts model weights from an OCI image.

                          This is the recommended source: it is deterministic, layer-cached by the
                          container runtime, works fully offline, and decouples the model's version
                          from the engine's — which is what allows a model change and an engine
                          change to be rolled out independently.
                        properties:
                          image:
                            description: Image is the OCI reference containing the
                              model file.
                            minLength: 1
                            type: string
                          path:
                            default: /weights/model.gguf
                            description: |-
                              Path is the absolute path to the model file inside that image.

                              This is where the file lives in the MODEL image, which is a different
                              place from where the engine will read it: the operator copies it onto a
                              shared volume and mounts that at a fixed location. The default matches
                              the convention used by this project's own Dockerfile.model.
                            pattern: ^/.*
                            type: string
                          pullPolicy:
                            default: IfNotPresent
                            description: PullPolicy for the model image.
                            enum:
                            - Always
                            - IfNotPresent
                            - Never
                            type: string
                        required:
                        - image
                        type: object
                      persistentVolumeClaim:
                        description: |-
                          PersistentVolumeClaim mounts weights from an existing PVC.
                          Not implemented yet.
                        properties:
                          claimName:
                            description: ClaimName is the PVC to mount.
                            minLength: 1
                            type: string
                          path:
                            description: Path is the absolute path to the model file
                              within the volume.
                            pattern: ^/.*
                            type: string
                          readOnly:
                            default: true
                            description: ReadOnly mounts the volume read-only.
                            type: boolean
                        required:
                        - claimName
                        - path
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of image, huggingFace or persistentVolumeClaim
                        must be set
                      rule: '[has(self.image), has(self.huggingFace), has(self.persistentVolumeClaim)].filter(x,
                        x).size() == 1'
                required:
                - name
                - source
                type: object
              replicas:
                default: 1
                description: |-
                  Replicas is the TOTAL number of serving pods across all variants
                  (primary + canary combined).

                  This field backs the /scale subresource, so `kubectl scale` and any
                  HorizontalPodAutoscaler targeting this resource write here. During a
                  canary the controller distributes this total between the two variants
                  rather than adding to it — an autoscaler decides how much capacity
                  exists, the rollout decides how it is split.

                  It is a pointer so that "unset" is distinguishable from zero, which
                  matters when an external autoscaler owns the field.
                format: int32
                minimum: 0
                type: integer
              rollout:
                description: |-
                  Rollout controls how replacements are rolled out when the pod template
                  changes.
                properties:
                  autoRollback:
                    default: true
                    description: |-
                      AutoRollback reverts to the last known-good revision when the deadline is
                      exceeded. Consumed from the sprint that implements rollback; declared
                      here so the field is stable.
                    type: boolean
                  maxSurge:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      MaxSurge is the number or percentage of pods that may exist above the
                      desired count during an update. Ignored when Type is Recreate.
                    x-kubernetes-int-or-string: true
                  maxUnavailable:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      MaxUnavailable is the number or percentage of pods that may be
                      unavailable during an update. Ignored when Type is Recreate.
                    x-kubernetes-int-or-string: true
                  progressDeadline:
                    default: 600s
                    description: |-
                      ProgressDeadline is how long a rollout may make no progress before it is
                      declared failed.
                    type: string
                  type:
                    default: RollingUpdate
                    description: Type selects the update strategy.
                    enum:
                    - RollingUpdate
                    - Recreate
                    type: string
                type: object
              serving:
                description: Serving configures the network surface in front of the
                  engine.
                properties:
                  port:
                    default: 8080
                    description: Port is the port the ModelDeployment's Service listens
                      on.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  startupTimeout:
                    default: 300s
                    description: |-
                      StartupTimeout bounds how long the engine may take to load its model and
                      report healthy before the pod is restarted.

                      Model load dominates startup and varies by orders of magnitude between
                      engines — seconds for a quantized GGUF, minutes for a framework that
                      compiles kernels first — so this is generous by default and is enforced
                      with a startupProbe rather than a livenessProbe.
                    type: string
                type: object
            required:
            - engine
            - model
            type: object
          status:
            description: status is the observed state.
            properties:
              availableReplicas:
                description: |-
                  AvailableReplicas is the number of pods that have been ready long enough
                  to count as available.
                format: int32
                type: integer
              conditions:
                description: Conditions follow the standard Kubernetes condition contract.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoint:
                description: Endpoint is the in-cluster OpenAI-compatible base URL.
                type: string
              lastGoodRevision:
                description: |-
                  LastGoodRevision is the most recent revision that reached availability.
                  It is the target an automatic rollback reverts to.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the .metadata.generation this status reflects.
                  Anything asking "is the rollout finished?" must compare this against
                  .metadata.generation first, or it will read a stale answer.
                format: int64
                type: integer
              phase:
                description: Phase is a coarse summary for humans and printer columns.
                enum:
                - Pending
                - Progressing
                - Available
                - Degraded
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of pods passing their readiness
                  probe.
                format: int32
                type: integer
              replicas:
                description: |-
                  Replicas is the TOTAL actual pod count across all variants.

                  Part of the /scale subresource contract. An HPA divides by this number,
                  so an inaccurate value does not merely look wrong, it makes the
                  autoscaler compute the wrong target.
                format: int32
                type: integer
              selector:
                description: |-
                  Selector is a SERIALIZED label selector string (e.g.
                  "llmcp.io/model-deployment=demo"), not a metav1.LabelSelector.

                  Part of the /scale subresource contract: an HPA uses it to find the pods
                  whose metrics it averages. It must select every variant, so that scaling
                  decisions are made over the whole fleet rather than half of it.
                type: string
              stableRevision:
                description: StableRevision is the revision hash currently serving
                  as primary.
                type: string
              updatedReplicas:
                description: UpdatedReplicas is the number of pods running the target
                  revision.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
